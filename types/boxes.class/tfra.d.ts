import { Mp4Box } from "./Mp4Box.class";
export declare class tfra extends Mp4Box {
    get track_ID(): number;
    set track_ID(value: number);
    get length_size_of_sample_num(): number;
    get length_size_of_trun_num(): number;
    get length_size_of_traf_num(): number;
    get number_of_entry(): number;
    set number_of_entry(value: number);
}
